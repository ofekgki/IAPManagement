package com.example.purchasebackend.domain.enums;

/**
 * How a purchase is fulfilled.
 *
 * <p>MOCK is fully functional for demo/testing/educational use. GOOGLE_PLAY is scaffolded but every
 * real Google step is a TODO; it fails safely until configured.
 */
public enum BillingMode {
    MOCK,
    GOOGLE_PLAY
}
