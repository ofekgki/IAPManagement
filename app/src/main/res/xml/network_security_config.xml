<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext (HTTP) traffic ONLY to the local development backend hosts, so the demo can reach
  a backend running on your computer during development. Everything else stays HTTPS-only.

  - 10.0.2.2   : the Android emulator's alias for the host machine's localhost.
  - localhost  : on-device loopback.
  TODO: For a real release, remove these and serve the backend over HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
