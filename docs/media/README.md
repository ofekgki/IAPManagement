# Media assets

Drop the screenshots and screen-recordings referenced by the root [`README.md`](../../README.md) here.
Keep the exact file names below so the README embeds resolve:

| File | What to capture |
|------|-----------------|
| `logo.png` | Project logo — shown at the top of the root [`README.md`](../../README.md) |
| `Explanation_Video.mp4` | Overview/explainer video — embedded at the top of the docs site (`docs/index.html`) |
| `portal-dashboard.png` | Portal → app Dashboard (KPIs + recent purchases) |
| `portal-revenue.png` | Portal → Revenue (over-time chart + by-payment-method pie) |
| `portal-purchases.png` | Portal → Purchases (paginated table) |
| `portal-apikeys.png` | Portal → API Keys (create / revoke / rotate) — used by the docs site (`docs/index.html`) |
| `demo-home.png` | Demo app → Home (gradient hero + section cards) |
| `demo-store.png` | Demo app → Store (product cards) |
| `demo-popup.png` | SDK purchase popup (product artwork hero) |
| `demo-walkthrough.mp4` | Short screen-recording of the demo app buy → restore flow |
| `portal-walkthrough.mp4` | Short screen-recording of the portal analytics |

Tips: use a phone/emulator frame for app shots, and export videos as `.mp4`. The docs site
(`docs/index.html`) embeds the `.mp4` files inline via `<video>` tags; the root `README.md` links to
them (GitHub only plays `.mp4` embeds when uploaded to a release/issue). PNGs render inline everywhere.
